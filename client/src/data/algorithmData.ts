// 算法题目内容数据
// 包含二分法分类下的详细题目

export interface AlgorithmExample {
  id: string;
  title: string;
  content: React.ReactNode;
}

export interface AlgorithmCategory {
  id: string;
  title: string;
  description: string;
  examples: AlgorithmExample[];
}

export interface AlgorithmSection {
  id: string;
  title: string;
  icon: string;
  description: string;
  iconBgColor: string;
  categories: AlgorithmCategory[];
}

// 二分法分类内容
export const binarySearchContent = {
  id: 'binary-search',
  title: '二分法',
  description: '二分查找的核心思想、模板和经典应用',
  examples: [
    {
      id: 'median-of-two-sorted-arrays',
      title: '例题1：寻找两个有序数组中位数的切分逻辑图解',
    },
  ],
};

// 例题1的详细内容 - 寻找两个有序数组中位数
export const medianOfTwoSortedArraysContent = `
## 寻找两个有序数组中位数的切分逻辑图解

在解决"寻找两个正序数组的中位数"时，我们使用二分查找来确定一个**切分位置**。理解这个问题的核心在于：\`i\` 不是数组的索引，而是"左半部分包含来自 nums1 的元素个数"。

---

### 1. 为什么 \`right = m\` 而不是 \`m - 1\`？

在常规的二分查找（寻找某个值）中，由于索引范围是 \`0\` 到 \`m-1\`，所以 \`right = m-1\`。

但在本算法中，我们在找的是**切分位置**。对于长度为 \`m\` 的数组，总共有 \`m + 1\` 个可能的切分位置：

**示例：** \`nums1 = [A, B, C]\` \`(m = 3)\`

可能的切分位置标记为 \`|\`：

| 位置 i | 切分状态 | 左侧元素 | 右侧元素 |
|--------|----------|----------|----------|
| 0 | \`'\` | \`ABC'\` | (无) |
| 1 | \`'A\` | \`BC'\` | A |
| 2 | \`'AB\` | \`C'\` | A, B |
| 3 | \`'ABC\` | \`'\` | A, B, C |

- 如果 \`i = 0\`，表示 \`nums1\` 的所有元素都被划分到了右半部分。
- 如果 \`i = m\`，表示 \`nums1\` 的所有元素都被划分到了左半部分。
- **结论：** 切分位置的取值范围是 \`[0, m]\`，共有 \`m + 1\` 种可能，所以二分查找的右边界必须是 \`m\`。

---

### 2. 图解：\`i == 0\` 或 \`i == m\` 是怎么发生的?

假设我们有两个数组，需要把它们合并后的总集切分成左右两半。

#### 场景 A: \`i == 0\`（nums1 的元素全在右边）

这种情况发生在 \`nums1\` 里的所有数都非常大，以至于左半部分只需要从 \`nums2\` 中取数。

\`\`\`
nums1 (m=3):       |  [ 10, 20, 30 ]  (i=0)
nums2 (n=5): [ 1, 2, 3, 4 | 5 ]        (j=4)
             -----------------
左半部: [ 1, 2, 3, 4 ]
右半部: [ 10, 20, 30, 5 ] (排序后为 5, 10, 20, 30)
\`\`\`

**此时：**
- \`maxLeft1\`：因为 \`i = 0\`，左边没东西，设为 \`-∞\`。
- \`minRight1\`：就是 \`nums1[0]\` (10)。

#### 场景 B: \`i == m\`（nums1 的元素全在左边）

这种情况发生在 \`nums1\` 里的所有数都非常小。

\`\`\`
nums1 (m=3): [ 1, 2, 3 | ]             (i=3)
nums2 (n=5): [ 4 | 5, 6, 7, 8 ]        (j=1)
             -----------------
左半部: [ 1, 2, 3, 4 ]
右半部: [ 5, 6, 7, 8 ]
\`\`\`

**此时：**
- \`maxLeft1\`：就是 \`nums1[m-1]\` (3)。
- \`minRight1\`：因为 \`i = m\`，右边没东西，设为 \`+∞\`。

---

### 3. 代码逻辑中的边界处理

当你理解了 \`i\` 是"个数"后，代码里的三元运算符就很好理解了：

\`\`\`java
// i 是切分位置，也等于 nums1 左侧元素的个数
// 如果 i == 0，说明 nums1 左侧没有元素，为了不影响 Math.max 比较，设为负无穷
int maxLeft1 = (i == 0) ? Integer.MIN_VALUE : nums1[i - 1];

// 如果 i == m，说明 nums1 右侧没有元素，为了不影响 Math.min 比较，设为正无穷
int minRight1 = (i == m) ? Integer.MAX_VALUE : nums1[i];
\`\`\`

---

### 4. 移动逻辑

#### 情况 1: \`maxLeft1 > minRight2\`

- **含义：** \`nums1\` 左边的数太大了，大过了 \`nums2\` 右边的数。
- **解决：** 我们需要让 \`nums1\` 贡献给左半部分的元素减少。
- **动作：** 切分点 \`i\` 必须向**左移动**。
- **代码：** \`right = i - 1\`。

\`\`\`
nums1: [ 1, 100 | 101 ]  (maxLeft1 = 100)
nums2: [ 2, 3 | 4, 5 ]   (minRight2 = 4)
由于 100 > 4，切分点必须左移，变成 [ 1 | 100, 101 ]
\`\`\`

#### 情况 2: \`maxLeft2 > minRight1\`（即代码中的 \`else\`）

- **含义：** \`nums2\` 左边的数太大了，甚至大过了 \`nums1\` 右边的数。
- **解决：** 我们需要让 \`nums1\` 贡献给左半部分的元素**增加**（从而让 \`nums2\` 贡献的减少）。
- **动作：** 切分点 \`i\` 必须向**右移动**。
- **代码：** \`left = i + 1\`。

---

### 总结

1. **范围 \`[0, m]\`：** 是因为我们要找的是"缝隙"的位置，长度为 \`m\` 的数组有 \`m + 1\` 条缝隙。

2. **\`i == 0\` 与 \`i == m\`：** 代表切分点在数组的最边缘。

3. **移动逻辑：**
   - \`maxLeft1 > minRight2\` → \`nums1\` 左边太强，\`i\` 左移 (\`right = i-1\`)。
   - \`maxLeft2 > minRight1\` → \`nums1\` 右边太强（或 \`nums2\` 左边太强），\`i\` 右移 (\`left = i+1\`)。

4. **i 与索引的关系：** \`i\` 位置左边的元素索引是 \`i - 1\`，右边的元素索引是 \`i\`。
`;
